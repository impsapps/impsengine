Following modules are needed:

YAML::XS -> only needed for GenerateFromYamlMain.pl

How to install:

Execute following commands from terminal:

1. cpan App::cpanminus
2. cpanm YAML::XS

See also: http://www.cpan.org/modules/INSTALL.html